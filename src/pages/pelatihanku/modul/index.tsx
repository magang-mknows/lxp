"use client";
import MyTrainingModulModules from "@/modules/my-training-module";
import React, { ReactElement } from "react";

const page = (): ReactElement => {
  return <MyTrainingModulModules />;
};

export default page;
