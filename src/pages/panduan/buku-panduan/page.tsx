import GuideBookModule from "@/modules/Guide/guide-book/GuideBookModule";
import React, { ReactElement, Suspense } from "react";

const Page = (): ReactElement => {
  return <GuideBookModule />;
};

export default Page;
