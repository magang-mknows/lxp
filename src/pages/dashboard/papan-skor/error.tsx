"use client";
import { FC, ReactElement } from "react";

const Error: FC = (): ReactElement => {
  return <span className="text-red-400 text-9xl">Error nya ada di leaderboard page</span>;
};

export default Error;
