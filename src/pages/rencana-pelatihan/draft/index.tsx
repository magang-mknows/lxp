import TrainingContractDraft from "@/modules/training-plan/draft";
import { FC, ReactElement } from "react";

const index: FC = (): ReactElement => {
  return <TrainingContractDraft />;
};

export default index;
