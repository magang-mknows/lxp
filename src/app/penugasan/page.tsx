import AssigmentModules from "@/modules/Assigment/AssigmentModule";
import React, { ReactElement } from "react";

const page = (): ReactElement => {
  return <AssigmentModules />;
};

export default page;
