import AssigmentLoading from "@/modules/Assigment/AssigmentLoading";
import React, { ReactElement } from "react";

const loading = (): ReactElement => {
  return <AssigmentLoading />;
};

export default loading;
