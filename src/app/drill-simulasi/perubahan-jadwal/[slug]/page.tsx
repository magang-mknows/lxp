import RequestReScheduleModule from "@/modules/drill-simulation/request-rescedule/RequestReScheduleModule";
import React, { FC, ReactElement } from "react";

const page: FC = (): ReactElement => {
  return <RequestReScheduleModule />;
};

export default page;
