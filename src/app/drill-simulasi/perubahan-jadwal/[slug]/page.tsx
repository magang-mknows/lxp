import RequestReScheduleModule from "@/modules/drill-simulation/request-rescedule";
import React, { FC, ReactElement } from "react";

const page: FC = (): ReactElement => {
  return <RequestReScheduleModule />;
};

export default page;
