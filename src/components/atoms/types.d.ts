export type TButtonProps = {
  type: "primary" | "secondary";
  text: string;
  size: "w-24" | "w-28" | "w-20";
};

export type TImageIDProps = {
  id: number;
};

export type TBenefitsProps = {
  title: string;
  desc: string;
};
