import { FC, ReactElement } from "react";

const HistorySimulationCardLoading: FC = (): ReactElement => {
  return (
    <section className="w-full min-h-[140px] mb-4 bg-neutral-200 animate-pulse rounded-md"></section>
  );
};

export default HistorySimulationCardLoading;
