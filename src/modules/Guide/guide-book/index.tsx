import GuideBookPages from "@/components/organism/guide-book/GuideBook";
import React, { ReactElement } from "react";

const GuideBookModule = (): ReactElement => {
  return <GuideBookPages />;
};

export default GuideBookModule;
